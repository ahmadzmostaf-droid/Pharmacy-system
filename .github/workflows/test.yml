name: CI_Testing
on: [push]
jobs:
  check-files:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify Project Files
        run: |
          ls index.html
          ls app.js